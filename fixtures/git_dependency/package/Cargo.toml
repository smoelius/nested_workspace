[package]
name = "package"
version = "0.1.0"
edition = "2024"
publish = false

[lib]
doctest = false

[dependencies]
nested_workspace = { git = "https://github.com/smoelius/nested_workspace" }

[workspace]
