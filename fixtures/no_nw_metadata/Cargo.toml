[package]
name = "no_nw_metadata"
version = "0.1.0"
edition = "2024"
publish = false

[build-dependencies]
nested_workspace = { path = "../.." }

[dev-dependencies]
nested_workspace = { path = "../.." }

[lib]
doctest = false
