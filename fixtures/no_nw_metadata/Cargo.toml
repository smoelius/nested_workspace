[package]
name = "no_nw_metadata"
version = "0.1.0"
edition = "2024"
publish = false

[build-dependencies]
nested-workspace = { path = "../../nested-workspace" }

[dev-dependencies]
nested-workspace = { path = "../../nested-workspace" }

[lib]
doctest = false
